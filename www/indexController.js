angular.module('cycard')

.controller('indexCtrl', ['$rootScope', '$state', '$animate', function($rootScope, $state, $animate) {

  // $rootScope.showNav = $state.current.name;
  // if ($rootScope.showNav)
  //   $('ion-header-bar').removeClass('ng-hide');
  // else
  //   $('ion-header-bar').addClass('ng-hide');
    // $animate.removeClass(ion-header-bar, 'ng-hide');

}]);